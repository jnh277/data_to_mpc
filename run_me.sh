pip install --upgrade pip # update pip for wheels support (mihgt be necessary)
pip install --upgrade jax jaxlib # install CPU jax (do not do this like this for GPU support)
pip install -r requirements.txt